# Sts2StateBridge v0.11.0

本版本补齐地图、事件和商店的常用操作闭环，并继续使用统一的安全动作接口。

## 新增功能

- 支持移动到所有当前可达的地图节点。
- 支持选择所有当前可见且未锁定的事件选项。
- 支持打开与关闭商店、购买卡牌/遗物/药水，以及离开商店房间。
- 支持购买删牌服务、选择并确认一张当前可移除的牌。
- 支持在卡牌结果展示页点击“√”确认并继续。
- 保留 `v0.10.0` 的战斗、奖励、药水、宝箱、休息点和锻造动作。
- 所有动作继续使用 `execute_action(state_id, action_id)`，并保留状态版本、白名单和单次消费保护。

## 安全边界

- 写操作默认关闭，必须通过本机配置明确启用。
- 远古遗物和尚未识别的特殊子界面仍为只读。
- 仅支持单人模式下当前 UI 可见且已启用的候选。
- 本项目不包含游戏程序集、游戏资产、API Key 或本机配置。

## 兼容性

- 目标游戏版本：STS2 `v0.111.0`
- Mod：`0.11.0`，.NET 9
- MCP Server：`0.11.0`，Python 3.11+ 与 uv

商店中显示为“空值”的商品是游戏内合法卡牌 `Null`，不是状态解析失败。
