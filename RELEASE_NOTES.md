# Sts2StateBridge v0.12.0

本版本完成战斗只读状态的第二版 schema，让 MCP Host 能更准确地理解临时选牌、动态卡牌属性、Power、敌人意图和当前输入状态。现有动作接口保持兼容，没有扩大写操作范围。

## 战斗读取

- `/snapshot.schema_version` 升级为 `2`，已有字段保持兼容。
- 新增 `combat.readiness`，提供玩家回合、输入锁定、动画、选择弹窗和稳定状态。
- 新增统一的 `combat.selection`，覆盖卡牌药水、卡牌组合、战斗牌堆选牌和手牌选择。
- 扩充卡牌类型、稀有度、卡池、关键词、升级、附魔、异变、基础/当前能量费用、星能费用，以及动态伤害和格挡。
- 扩充玩家和敌人的 Power 明细、敌人可见状态及结构化意图效果。
- 新增当前敌方总攻击和格挡抵消后的预计未防伤害等确定性派生值。
- 所有新增内容仍只限当前 UI 可见信息，不读取 RNG、未知抽牌顺序或敌人未来招式。

## MCP 与 Agent

- MCP Server 升级到 `0.12.0`，`get_combat_state` 保留新增战斗字段。
- `get_full_snapshot` 继续返回完整原始快照。
- `execute_action(state_id, action_id)` 及其安全边界保持不变。
- 可选 Agent 升级到 `0.7.0`，加入可替换的战士策略 Skill；该 Agent 不包含在 MCP Release ZIP 中。

## 安全与兼容性

- 写操作默认关闭，必须通过本机配置明确启用。
- 仅支持单人模式下当前 UI 可见且已启用的动作候选。
- 目标游戏版本：STS2 `v0.111.0`。
- Mod：`0.12.0`，.NET 9。
- MCP Server：`0.12.0`，Python 3.11+ 与 uv。
- 发布包不包含游戏程序集、游戏资产、API Key、虚拟环境或缓存。
