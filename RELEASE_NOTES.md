# Sts2StateBridge v0.10.0

本版本为 AI 增加首批非战斗操作闭环，并继续使用统一的安全动作接口。

## 新增功能

- `interaction.actions` 返回当前非战斗界面的合法动作候选。
- 支持领取战斗奖励以及完成后的继续。
- 支持选择或跳过卡牌奖励。
- 药水槽已满时可先选择并丢弃已有药水，再重新读取并领取奖励药水。
- 支持开启宝箱、领取遗物和继续。
- 支持休息点选项和锻造选牌。
- 所有动作继续使用 `execute_action(state_id, action_id)`，并保留状态版本、白名单和单次消费保护。

## 安全边界

- 写操作默认关闭，必须通过本机配置明确启用。
- 地图、事件、商店和远古遗物仍为只读。
- 仅支持单人模式下当前 UI 可见且已启用的候选。
- 本项目不包含游戏程序集、游戏资产、API Key 或本机配置。

## 兼容性

- 目标游戏版本：STS2 `v0.111.0`
- Mod：`0.10.0`，.NET 9
- MCP Server：`0.10.0`，Python 3.11+ 与 uv

`v0.10.0` 只会在全部新增动作完成游戏内监督验收后发布。
